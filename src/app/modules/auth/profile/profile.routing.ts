import { Route } from '@angular/router';
import { ProfileComponent } from './profile.component'; 

export const authprofileRoutes: Route[] = [
    {
        path     : '',
        component: ProfileComponent
    }
];
